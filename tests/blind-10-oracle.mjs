// Blind #10 uses the independent arithmetic oracle that predates this question
// set. The dependency is included in the Blind #10 seal, so its calculations
// cannot change between suite construction and the untouched first run.
export * from "./blind-8-oracle.mjs";
