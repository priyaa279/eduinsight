# Optional: enables the flexible language-planning layer.
# Calculations always remain deterministic and are performed against the
# governed aggregate dataset inside EduInsight.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
